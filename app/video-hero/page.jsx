import VideoHeroClient from './VideoHeroClient'

export const metadata = {
  title: 'Video Hero — Test',
  robots: 'noindex, nofollow',
}

export default function Page() {
  return <VideoHeroClient />
}
